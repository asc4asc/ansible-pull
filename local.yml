---
# Playbook nur für Benutzerverwaltung (ohne SSH-Härtung)

- name: Nur Benutzer anlegen
  hosts: localhost

  # become: true
  gather_facts: true
  remote_user: root

  roles:
    - role: add_user
# End of file
